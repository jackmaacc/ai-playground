# Third-party tool installs (their own repos, huge, not ours to track)
chat-llm/
image-gen/

# TLS interception workaround artifacts, not project content
*.pem
*.crt

# Python
__pycache__/
*.pyc
.pytest_cache/

# Machine-specific runtime state: which app the manager started, its pid,
# and the launch logs. Meaningless on any other machine.
learning/.runtime/

# Runtime logs from launching tools
*_log.txt

# User's own separate experiment/repo, not part of this project
AiFun/
